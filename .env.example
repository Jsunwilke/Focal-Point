# Firebase Configuration
# Copy this file to .env.local (for development) and/or .env.production (for builds)
# Fill in your actual values. DO NOT commit real secrets.

# Firebase API Key
REACT_APP_FIREBASE_API_KEY=your_api_key_here

# Firebase Auth Domain (usually your-project-id.firebaseapp.com)
REACT_APP_FIREBASE_AUTH_DOMAIN=your_auth_domain_here

# Firebase Project ID
REACT_APP_FIREBASE_PROJECT_ID=your_project_id_here

# Firebase Storage Bucket (usually your-project-id.firebasestorage.app)
REACT_APP_FIREBASE_STORAGE_BUCKET=your_storage_bucket_here

# Firebase Messaging Sender ID
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here

# Firebase App ID
REACT_APP_FIREBASE_APP_ID=your_app_id_here

# Firebase Measurement ID (for Google Analytics)
REACT_APP_FIREBASE_MEASUREMENT_ID=your_measurement_id_here

# Stream Chat Configuration
# Get these from your GetStream.io dashboard
# REQUIRED: Get your API key from https://getstream.io/dashboard/
REACT_APP_STREAM_KEY=your_stream_api_key_here
# WARNING: Do not expose STREAM secrets in production builds.
# Optional dev-only secret to enable local token generation:
REACT_APP_STREAM_SECRET=your_stream_secret_here_dev_only
# Optional: Stream Chat API base URL (defaults to getstream.io)
REACT_APP_STREAM_BASE_URL=https://chat.stream-io-api.com

# Google Maps API Key (for location features)
REACT_APP_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Giphy API Key (for GIF search in chat)
REACT_APP_GIPHY_API_KEY=your_giphy_api_key_here

# Captura (client-side)
# Account identifier used by the app when calling your backend
REACT_APP_CAPTURA_ACCOUNT_ID=your_captura_account_id_here

# Database (Prisma)
# Used by Prisma tools/scripts; never commit real URLs
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Backend (Firebase Functions) — set as deployment env/secrets, not in client builds
# These are read by code in functions/ via process.env
# Stream Chat (server)
# STREAM_KEY=your_stream_api_key
# STREAM_SECRET=your_stream_api_secret
# Captura (server)
# CAPTURA_CLIENT_ID=your_captura_client_id
# CAPTURA_CLIENT_SECRET=your_captura_client_secret
# CAPTURA_ACCOUNT_ID=your_captura_account_id
# SMTP (email service)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=notifications@example.com
# SMTP_PASS=your_smtp_password
# FROM_EMAIL=notifications@example.com
# FROM_NAME=Focal Point Studio
# Rebuild webhook token (if used)
# REBUILD_TOKEN=replace-with-random-secret
